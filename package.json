{
  "name": "wanderlust",
  "version": "1.0.0",
  "scripts": {
    "start": "npx @11ty/eleventy --serve --watch",
    "build": "rm -rf ./_site && npx @11ty/eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "2.0.1"
  }
}
